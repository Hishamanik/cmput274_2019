#ifndef _AVERAGE_H_
#define _AVERAGE_H_

#include "record.h"

// assumes num >= 1, returns the average of the student marks
// as a double (a floating point type, e.g. 73.67)
double calcAverage(StudentRecord students[], int num);

#endif
