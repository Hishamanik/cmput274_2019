#include <iostream>
#include <algorithm> // for max()

using namespace std;

int main() {
  // declare variables
	int y = 0;
	int x = [];

  // read the input
	cin >> y ;
	for len(y){ 
		cin >> x ;
	}

  // compute the answer
	int count = 0;
	for (i=0, i=y, i++){
		if (x[i] = x[i+1]) {
			count +=1
		}
		

	}

  // print the output
	cout << 

  return 0;
}
