#include <iostream>
#include <algorithm> // for max()

using namespace std;

int main() {
  // declare your variables
	int numbers;
	int stairs = [];
  // read the input
	for (i=0, i= numbers, i++){
		cin >> stairs;
	}

  // compute the answer
	int maximum_height = max(stairs);
	int minimum_height = min(stairs);
  // find the index of max stairs and start a loop from there to the minimum height
	

  // output the answer
	cout << maximum_height;

  return 0;
}
